# OculusGO Unity VR template

To start:

```bash
git clone https://github.com/Kif11/oculusgo-unity-template
cd oculusgo-unity-template
sudo chmod +x util.sh
./util.sh init
```

Open Unity, press File > Build And Run
